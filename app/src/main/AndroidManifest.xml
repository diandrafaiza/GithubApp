<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.submission3_diandra">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Submission3_Diandra">
        <activity
            android:name=".theme.ThemeActivity"
            android:label="@string/setting"
            android:exported="false" />
        <activity
            android:name=".favorite.ui.FavoriteActivity"
            android:label="@string/favorite2"
            android:exported="false" />
        <activity
            android:name=".detailuser.DetailUserActivity"
            android:label="@string/detail_user"
            android:exported="false" />
        <activity
            android:name=".main.MainActivity"
            android:label="@string/app_name"
            android:exported="false" />
        <activity
            android:name=".main.SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>